<% if flash.present? %>
	<div class='row'>
		<div class="col-lg-10 col-lg-offset-1 ">
			<% flash.each do |name, msg| %>
				<div class="alert alert-dissmissable <%= alert_class(name) %>">
					<%= content_tag :p, msg %>
				</div>
			<% end %>
		</div>
	</div>
<%end%>